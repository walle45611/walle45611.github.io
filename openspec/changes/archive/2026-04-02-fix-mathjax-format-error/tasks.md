## 1. OpenSpec Artifacts

- [x] 1.1 Create the `fix-mathjax-format-error` proposal, design, and spec artifacts for the MathJax rendering fix
- [x] 1.2 Create implementation tasks that cover article cleanup and verification

## 2. Article Cleanup

- [x] 2.1 Update the Extended Master Theorem subsection in `source/_posts/複雜度計算.md` to use consistent MathJax-friendly inline and block formulas
- [x] 2.2 Fix nearby math expressions in the same post that use the same mixed prose and raw operator formatting pattern

## 3. Verification

- [x] 3.1 Run `npm run build` to verify the post still renders successfully in the Hexo build
- [x] 3.2 Review the resulting changes to confirm only the intended OpenSpec artifacts and article math formatting were updated
