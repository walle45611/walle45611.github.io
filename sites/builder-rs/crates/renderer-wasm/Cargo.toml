[package]
name = "renderer-wasm"
description = "Reusable markdown renderer (native + optional wasm entrypoint)."
authors = ["LLM Wiki"]
edition.workspace = true
version.workspace = true
license.workspace = true

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
comrak.workspace = true
wasm-bindgen = "0.2"
